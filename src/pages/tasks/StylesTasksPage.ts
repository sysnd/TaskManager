import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => {
    return {
        container: {
            width: '100%',
            boxSizing: 'border-box',
            maxWidth: '1248px',
            margin: 'auto',
            marginTop: '100px'
        }
    };
});